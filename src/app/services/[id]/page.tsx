import ServiceMain from '@/components/UI/ServiceMain';
import React from 'react'

const page = () => {
    return (
        <div>
            <ServiceMain />
        </div>
    );
};

export default page;